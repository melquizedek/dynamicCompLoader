import { TempLoaderDirective } from './temp-loader.directive';

describe('TempLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TempLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
